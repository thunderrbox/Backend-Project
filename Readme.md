# Backend Project

This is a backend project built using Node.js and Express.js. It provides APIs for managing users, products, and orders. The project uses MongoDB as the database and Mongoose for object data modeling.
